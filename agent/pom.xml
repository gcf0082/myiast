<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.iast</groupId>
    <artifactId>iast-agent</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>IAST Agent</name>
    <description>Java Agent for IAST monitoring</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <bytebuddy.version>1.14.10</bytebuddy.version>
    </properties>

    <dependencies>
        <!-- 公共 SDK：IastPlugin / MethodContext / LogWriter / EventWriter 等
             插件作者会直接引这个模块；agent 内部也依赖它编译通过。shade 时会把 SDK 类打进 fat jar，
             运行期位于 bootstrap CL，外部插件 URLClassLoader 的 parent 委派能找到同一份 Class. -->
        <dependency>
            <groupId>com.iast</groupId>
            <artifactId>iast-sdk</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>net.bytebuddy</groupId>
            <artifactId>byte-buddy</artifactId>
            <version>${bytebuddy.version}</version>
        </dependency>
        <!-- Byte Buddy Agent：跨平台 attach 兜底（Linux/macOS UNIX Socket + Windows JNA DLL 注入） -->
        <dependency>
            <groupId>net.bytebuddy</groupId>
            <artifactId>byte-buddy-agent</artifactId>
            <version>${bytebuddy.version}</version>
        </dependency>
        <!-- JNA：byte-buddy-agent 在 Windows 平台 attach 时用来调 Win32 API（OpenProcess/CreateRemoteThread） -->
        <dependency>
            <groupId>net.java.dev.jna</groupId>
            <artifactId>jna</artifactId>
            <version>5.14.0</version>
        </dependency>
        <dependency>
            <groupId>org.ow2.asm</groupId>
            <artifactId>asm</artifactId>
            <version>9.5</version>
        </dependency>
        <dependency>
            <groupId>org.ow2.asm</groupId>
            <artifactId>asm-commons</artifactId>
            <version>9.5</version>
        </dependency>
        <!-- YAML解析库 -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>2.2</version>
        </dependency>
        <!-- Servlet API -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>6.0.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                    <encoding>UTF-8</encoding>
                    <compilerArgs>
                        <arg>--add-modules</arg>
                        <arg>jdk.attach</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- 把 src/wrapper-java 作为额外源目录编译；之后 antrun 会把产物挪走避免进主 classpath -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>add-wrapper-source</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.basedir}/src/wrapper-java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--
                wrapper 类不能被 bootstrap ClassLoader 当成普通 class 加载（会 NoClassDefFoundError：
                bootstrap 看不到 jakarta.servlet.*）。所以编译完先搬出 target/classes，改名为 .class.bin 资源，
                运行期通过 getResourceAsStream 取字节，再由 WrapperInjector 注入目标应用 ClassLoader。
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>hide-wrapper-class</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <move file="${project.build.outputDirectory}/com/iast/agent/runtime/jakarta/BufferingHttpServletRequestWrapper.class"
                                      tofile="${project.build.outputDirectory}/iast/runtime/jakarta-BufferingHttpServletRequestWrapper.class.bin"/>
                                <move file="${project.build.outputDirectory}/com/iast/agent/runtime/jakarta/BufferingHttpServletRequestWrapper$CachedServletInputStream.class"
                                      tofile="${project.build.outputDirectory}/iast/runtime/jakarta-BufferingHttpServletRequestWrapper$CachedServletInputStream.class.bin"/>
                                <!-- 清空留下的空目录 -->
                                <delete dir="${project.build.outputDirectory}/com/iast/agent/runtime" quiet="true"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <manifestEntries>
                                        <Premain-Class>com.iast.agent.IastAgent</Premain-Class>
                                        <Agent-Class>com.iast.agent.IastAgent</Agent-Class>
                                        <Main-Class>com.iast.agent.AttachTool</Main-Class>
                                        <Can-Retransform-Classes>true</Can-Retransform-Classes>
                                        <Can-Redefine-Classes>true</Can-Redefine-Classes>
                                        <Can-Set-Native-Method-Prefix>true</Can-Set-Native-Method-Prefix>
                                        <Boot-Class-Path>iast-agent.jar</Boot-Class-Path>
                                    </manifestEntries>
                                </transformer>
                            </transformers>
                            <finalName>iast-agent</finalName>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
